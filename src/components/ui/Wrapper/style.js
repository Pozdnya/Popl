import { makeStyles } from "@material-ui/core";

export const useStyle = makeStyles((theme) => ({
  wrapper: {
    width: "85vw",
    height: "100vh",
    margin: "0 auto",
  },
  container: {
    display: "flex",
  },
}));
